using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClassesUi : MonoBehaviour
{
  /*  public List<UpgradeScriptableObject> UpgadeInfoList;
    [SerializeField] private GameObject highlightImage;
    public void PointerEnter()
    {
        if (highlightImage != null)
            highlightImage.SetActive(true);
        transform.localScale = new Vector2(1.1f, 1.1f);

        TooltipManager.instance.ToolTipObject.SetActive(true);
            
    }
    public void PointerExit()
    {
        if (highlightImage != null)
            highlightImage.SetActive(false);
        transform.localScale = new Vector2(1f, 1f);
        TooltipManager.instance.ToolTipObject.SetActive(false);
    }
    public void SelectClasse()
    {
        GameManager.Instance.CanSpawn = true;
        UpgradeManager.Instance.UpgadeToSpawn = UpgadeInfoList;
        transform.parent.gameObject.SetActive(false);
    }*/
}
